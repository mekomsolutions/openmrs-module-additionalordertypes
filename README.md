# OpenMRS Additional Order Types Module

This minimal OpenMRS module starts without adding UI or database changes and writes the following INFO message when OpenMRS starts it:

```text
Additional Order Types module loaded successfully
```
